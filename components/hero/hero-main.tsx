import React from 'react';
import HeroCarousel from './hero-carousel';
import ContainerWrapper from '../others/container-wrapper';

const HeroMain = () => {
    return (
        <ContainerWrapper>
            <HeroCarousel />
        </ContainerWrapper>
    );
};

export default HeroMain;